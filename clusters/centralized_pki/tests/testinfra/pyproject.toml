[project]
name = "centralized-pki-tests"
version = "0.0.0"
description = "testinfra verify suite for the centralized_pki Multipass cluster"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8",
    "pytest-testinfra>=10",
    "pytest-xdist>=3",
]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["."]
