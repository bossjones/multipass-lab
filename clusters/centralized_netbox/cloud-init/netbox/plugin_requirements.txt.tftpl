# Extra pip packages baked into the custom netbox-docker image (Dockerfile-Plugins reads this).
# Pinned to pair with the NetBox 4.4.x discovery pin — confirm the exact pairing in Phase 0
# (compat table: NetBox 4.4.10 -> plugin 1.7.0). See specs/netbox-discovery.md.
netboxlabs-diode-netbox-plugin==${diode_plugin_version}
