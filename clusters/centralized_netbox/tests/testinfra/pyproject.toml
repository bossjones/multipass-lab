[project]
name = "centralized-netbox-tests"
version = "0.0.0"
description = "testinfra verify suite for the centralized_netbox Multipass cluster"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8",
    "pytest-testinfra>=10",
    "pytest-xdist>=3",
    "httpx>=0.27",
]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["."]
